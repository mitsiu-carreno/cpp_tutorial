#include <iostream>

int main(){
  // It is possible to declare a pointer to a pointer to a pointer
  int ***ptrx3;

  // These can be used to dynamically allocate a three-dimensional array. However, doing so would require 
  // a loop inside a loop
  
  // You can even declare a pointer to a pointer to a pointer to a pointer
  
  int ****ptrx4;

  // Or higher if you wish

  return 0;
}
